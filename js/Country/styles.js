import {
  StyleSheet,
} from 'react-native';

import commonStyles from '../styles';

const styles = StyleSheet.create({
  ...commonStyles,
  featured: {
    width: 320,
    height: 200
  }
});

export default styles;
